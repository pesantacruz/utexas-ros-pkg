<launch>

  <arg name="world" default="ens32_world" />
  <arg name="x" default="5" />
  <arg name="y" default="5" />
  <arg name="personid" default="person" />
  <arg name="tf_prefix" default="" />
  <arg name="has_tf_prefix" default="false" />

  <!-- push robot_description to factory and spawn robot in gazebo -->
  <param name="person_description" command="cat $(find bwi_gazebo)/urdf/person.urdf" />
  <node name="spawn_robot" pkg="gazebo" type="spawn_model"
    args="-param person_description
    -urdf
    -x $(arg x)
    -y $(arg y)
    -z 0
    -model $(arg personid)
    -gazebo_namespace /$(arg world)"
    respawn="false" output="screen">
  </node>

</launch>
